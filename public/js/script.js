$( document ).ready(function() {
    $('.eliminar').submit(function(){
    	return true;
    	/*
    	var seguro = confirm("Seguro? ");
		var idUser = $(this).attr('id');
		if(seguro){
			$("tr#"+idUser).remove();
		}
		return seguro;
		*/
	});
});


